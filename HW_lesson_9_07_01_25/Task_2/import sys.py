import sys

# Додавання нового шляху
новий_шлях = "/шлях/до/вашої/теки"
if новий_шлях not in sys.path:
    sys.path.append(новий_шлях)


import sys

# Видалення шляху
шлях_для_видалення = "/шлях/до/вашої/теки"
if шлях_для_видалення in sys.path:
    sys.path.remove(шлях_для_видалення)


import sys

# Вставка шляху на початок списку
новий_шлях = "/шлях/до/вашої/теки"
if новий_шлях not in sys.path:
    sys.path.insert(0, новий_шлях)

